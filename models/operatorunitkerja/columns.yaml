columns:
    # unit_kerja_id:
    #     label: unit_kerja_id
    #     type: number
    #     searchable: true
    usernya:
        label: Operator Unit
        relation: user
        select: concat(first_name, ' ', last_name)
        searchable: true
    created_at:
        label: created_at
        type: datetime
        invisible: true
    updated_at:
        label: updated_at
        type: datetime
        invisible: true
